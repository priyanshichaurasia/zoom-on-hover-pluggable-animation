import { ZoomhoverDirective } from './zoomhover.directive';

describe('ZoomhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomhoverDirective();
    expect(directive).toBeTruthy();
  });
});
